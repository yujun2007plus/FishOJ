# 复制为 .env 后填入真实值。.env 已被 gitignore，不会提交。
# Caddy 对外端口（本机 80/443 被 Steam++ 等占用时用 8080）
CADDY_PORT=8080

# 以下 AI 密钥可选：留空则 AI 插件不可用，不影响做题/评测
DEEPSEEK_API_KEY=
BUILTIN_API_KEY=
FISHOJ_AI_API_KEY=
OPENAI_API_KEY=
FISHOJ_AI_BASE_URL=
FISHOJ_AI_MODEL=
